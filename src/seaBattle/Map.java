package seaBattle;

public class Map {
    // создать матрицу из placeable
    // разместить на матрице корабль и рядом аптечку
    // через карту добраться до корабля и узнать жив ли он?
    public static final int N = 10;
    private Placeable[][] map = new Placeable[N][N];

    public Map() {
        for (int i = 0; i < N; i++) {
            for (int j = 0; j < N; j++) {
                //hw
            }
        }
    }
}
